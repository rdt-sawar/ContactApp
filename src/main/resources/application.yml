server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.h2.Driver
    password: sa
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      enabled: true

app:
  messages:
    contactSaveSuccess: Contact saved successfully...
    contactSaveFail: Contact saved fail...
    contactDeleteSuccess : contact deleted successfully...
    contactDeleteFail : contact deletion failed...
    
